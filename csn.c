#include "main.h"
/**
 * csn - cast number
 * @n: number
 * @m: size
 * Return: casted value
 */
long int csn(long int n, int m)
{
	if (m == ml)
		return (n);
	else if (m == ms)
		return ((short)n);

	return ((int)n);
}
